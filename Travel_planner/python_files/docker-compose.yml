services:
  pathway:
    image: pathwaycom/pathway:latest
    container_name: pathway-vectorstore
    working_dir: /app
    volumes:
      - ./:/app
    command: ["python", "pathway_service.py"]
    ports:
      - "8765:8765"
